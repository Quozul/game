[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14", features = ["dynamic_linking"] }
bevy_quinnet = "0.9.0"
bevy_rapier2d = "0.27.0"
quinn = "0.11"
rand = "0.8.5"
serde = { version = "1.0", features = ["derive"] }
