[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.12", features = ["dynamic_linking"] }
bevy_quinnet = "0.6.0"
bevy_rapier2d = "0.24.0"
quinn = "0.10.1"
rand = "0.8.5"
serde = { version = "1.0.171", features = ["derive"] }
